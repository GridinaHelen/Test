CREATE DATABASE blog;
use blog;
CREATE TABLE IF NOT EXISTS post(id_post INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
name text);
CREATE TABLE IF NOT EXISTS message(id_message INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
mess VARCHAR(40),id_post INT UNSIGNED NOT NULL, posted DATETIME NOT NULL,
FOREIGN KEY (id_post) REFERENCES theme(id_post));
